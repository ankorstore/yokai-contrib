app:
  env: test
modules:
  log:
    level: debug
    output: test
  gcppubsub:
    factory:
      retries: 3
      interval: 1
    healthcheck:
      topics:
        - test-topic
      subscriptions:
        - test-subscription
